#include <stdio.h>

main()
{
    /*  Exercise 1-6
        Checking if the input is either 1 or 0  */
    printf("The output of getchar() != EOF is %d!!!\n", getchar() != EOF);

    /* Exercise 1-6
       Printing the value of EOF */
    printf("The value of EOF is %d!!!\n", EOF);
    /* Comes out -1 */
}
